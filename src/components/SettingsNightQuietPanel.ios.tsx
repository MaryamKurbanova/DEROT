/** iOS: category/app picker removed — Screen Time tracks all apps by default. */
export function SettingsNightQuietPanel(_props: {
  nightEnabled: boolean;
  onSelectionChanged: () => void;
}) {
  return null;
}
